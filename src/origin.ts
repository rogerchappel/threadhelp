export function isOriginAllowed(origin: string, allowedOrigins: string[]): boolean { let parsed: URL; try { parsed = new URL(origin); } catch { return false; } return allowedOrigins.some((allowed) => { if (allowed === "*") return true; if (allowed.startsWith("*.")) { const suffix = allowed.slice(1); return parsed.hostname.endsWith(suffix) && parsed.hostname !== allowed.slice(2); } try { const allowedUrl = new URL(allowed); return parsed.origin === allowedUrl.origin; } catch { return parsed.hostname === allowed; } }); }
